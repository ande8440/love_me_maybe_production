<%= form_for(@friend_saying) do |f| %>
  <% if @friend_saying.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@friend_saying.errors.count, "error") %> prohibited this friend_saying from being saved:</h2>

      <ul>
      <% @friend_saying.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :saying %><br>
    <%= f.text_field :saying %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
